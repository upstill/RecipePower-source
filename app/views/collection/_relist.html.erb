<%# Render the current collection in small_pic mode %>
	<% for @recipe in @collection.results_paged  %>
	  <% @recipe.current_user = @user_id %>
	  <%= render "shared/recipe_smallpic" %>
	<% end %>
	<div class="digg_pagination">
		<%= pagination_links if @collection.npages > 1 %>
	</div>
