<%# dialogHeader :captureRecipe, "Cookmark Captured", area: @_area %>
<%= dialogHeader :captureRecipe, "Cookmark Captured", response_service.modal_options %>
Sorry, HTML format not supported for recipe capture.
<!--
<%= form_errors_helper f, @recipe %>
<%= form_for @recipe, html: { class: "capture_recipe json" } do |f| %>
	<div class="recipe_comment_div" >
	   <%= f.label :comment, "Notes", :class=>"recipe_field_label" -%>
	   <br><%= f.text_area :comment, placeholder: "What are your thoughts about this recipe?" %>
	</div>
    <%= f.submit "Save", class: "save-tags-button" %>
    <%= f.submit "Cancel", class: "save-tags-button cancel" %>
<% end %>
-->
<%= dialogFooter %>

