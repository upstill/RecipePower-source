  <div class="content-item" style="width:100%">
    <% entity = decorator.object %>
    <% presenter = present(entity) %>
    <%= presenter.content_preface if policy(entity).update? %>
    <div id="rp-html-content">
      <%= presenter.html_content %>
    </div>
  </div>
