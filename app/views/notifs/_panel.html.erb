<div class="new-style notifs" , onload="RP.access_links.onload(event);">
  <div class="modal-content">
    <div class="modal-header" style="text-align: center; border: none; color:#ECE6CD;">
      <% if invitee %>
          <a class="select-content signup" data-activate="accept"><%= invitation_acceptance_label %></a>
          <a class="select-content rollup none"><%= sprite_glyph(:'vote-up', 'xl') %></a>
          <br><a class="select-content signin" data-activate="signin">Sign In Otherwise</a>
      <% else %>
          <a class="select-content signup" data-activate="signup">Sign Up</a>&nbsp;|&nbsp;
          <a class="select-content signin" data-activate="signin">Sign In</a>
          <a class="select-content rollup none" <%= 'style=display:initial;' if invitee_error %> >
            <%= sprite_glyph(:'vote-up', 'xl') %>
          </a>
      <% end %>
    </div>
    <div class="selectable modal-body <%= invitee ? 'accept' : 'signup' %>" <%= 'style=display:initial;' if invitee_error %> >
      <%= flash_notifications_div %>
      <% if invitee %>
          <%= render invitation_acceptance_form, resource: invitee, resource_name: 'user' %>
      <% else %>
          <%= render 'registrations/options' %>
      <% end %>
    </div>
    <div class="selectable modal-body signin">
      <%= flash_notifications_div %>
      <%= render 'sessions/form' %>
      <hr style="margin-top: 5px;">
      <%= render 'authentications/possible', prompt: 'OR SIGN IN VIA:' %>
    </div>
  </div>
</div>
