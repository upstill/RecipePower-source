<p>Calling <%= @resource.email %>!</p>

<p>
<%= (@resource.invited_by_id && (User.find(@resource.invited_by_id).fullname || User.find(@resource.invited_by_id).email)) || "Someone" %>
has invited you to join them on 
<%= link_to "RecipePower.com", root_url %>
<%= @resource.invitation_message ? ", saying \"#{@resource.invitation_message}\".<br>".html_safe : ". " %> 
You can accept through the link below.</p>

<p><%= link_to 'Accept invitation', accept_invitation_url(@resource, :invitation_token => @resource.invitation_token) %></p>

<p>If you don't want to accept the invitation, you can safely ignore this email.<br />
Your account won't be created until you access the link above and set your password.</p>
