<div class="signin_hdr">
	<div style="float:left"><%= image_tag "RPlogo.png", class: "small_logo", :alt => "RecipePower" %></div>
	<div class="hdrdiv">&nbsp;will reset your password if you're a member. Not a member? <u><%= link_to_submit "Sign up!",  new_user_registration_path, :mode => :modal %></u></div>
</div>
<%= flash_all false %>
<%= form_for(resource, :as => resource_name, :url => password_path(resource_name, mode: "injector"), :html => { class: "password_reset json", 'data-type' => 'json'} ) do |f| %>
	<div class="get_password">
		<%# form_errors_helper f %>
		<div class="signin_credentials">
		  <%= f.label :login, "Username or Email (your choice)".html_safe %>
		  <%= f.text_field :login, class: "signin_element" %>
		</div>
		<%= f.submit "Email instructions to reset password", class: "go_button" %>
	</div>
<% end %>
