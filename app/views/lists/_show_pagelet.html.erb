<h3>List '<%= @list.name %>'</h3>

<p>
  <%= @list.description %>
</p>

<p>
  <%= @list.notes %>
</p>

<% permitted_to?(:edit, :lists) do %>
    <%= link_to_modal 'Edit', edit_list_path(@list), class: "btn" %>
<% end %>
<%= link_to 'Back to Lists', lists_path, class: "btn" %>

<%= render "stream_results_placeholder" %>