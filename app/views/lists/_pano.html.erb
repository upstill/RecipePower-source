<div class="slider pano">
  <div class="track">
    <% ((defined?(list) && ListServices.new(list).tagging_scope(3).to_a.map(&:entity).uniq.keep_if { |entity| entity.picture_id != nil }[0..9]) || []).each do |member| %>
        <%= render_item member, :slider %>
    <% end %>
  </div>
</div>