<%# Fields for a new-feed dialog %>
<%= modal_dialog 'new_list new-style green',
                 (@first_entity ?
                         ("Start a list with "+@first_entity.decorate.title.truncate(30)) :
                         'Make a New Treasury') do %>
    <%= render 'form', decorator: @decorator %>
<% end %>
