<%= form_for @feed, html: {class: "new_feed json", 'data-type' => :json} do |f| %>
    <%= form_errors_helper f %>
    <%= render 'form_content', decorator: @decorator, f: f %>
    <%# f.button :submit, "Save" %>
    <%= form_actions f, cancel_path: feeds_path %>
<% end %>
