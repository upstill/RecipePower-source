<ul class="nav nav-pills <%= defined?(klass) && klass %>">
  <li role="presentation" class="dropdown">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
      <%= defined?(label) ? label : 'No label defined in shared/hover_menu' %><span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
      <% links.each do |link| %>
        <li><%= link %></li>
      <% end %>
    </ul>
  </li>
</ul>