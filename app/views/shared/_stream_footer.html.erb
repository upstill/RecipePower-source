<div class="row stream-footer", id="stream-footer">
    <div class="col-md-3">
      <% if @sp.next_path %>
          <%= stream_link @sp.next_path %>
      <% else %>
          No mo' stuff.
      <% end %>
    </div>
</div>
