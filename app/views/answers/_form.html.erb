<%= simple_form_for(@answer) do |f| %>
  <%= form_errors_helper f %>

  <div class="form-inputs">
    <%= f.input :answer %>
    <%= f.association :user %>
    <%= f.input :question_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
