<% if response_service.injector? %>
	<%= dialogHeader :heads_up, @title || express_error_context(@resource), class: "generic_alert" %>
		<h3><%= @title || express_error_context(@resource) %></h3>
		<div class="error_box">
			<%= express_resource_errors @resource, preface: (@title ? express_error_context(@resource) : "") %>
		</div>
		<div class="error_cancel">
			<%= dialog_cancel_button "Okay" %>
		</div>
	<%= dialogFooter %>
<% else %>
	<%= modal_dialog :heads_up, @title || express_error_context(@resource) do %>
		<%= modal_body do %>
			<%= express_resource_errors @resource, preface: (@title ? express_error_context(@resource) : "") %>
		<% end %>
		<%= modal_footer do %>
			<%= dialog_cancel_button "Okay" %>
		<% end %>
	<% end %>
<% end %>

