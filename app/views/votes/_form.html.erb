<%= simple_form_for(@vote) do |f| %>
  <%= form_errors_helper f %>

  <div class="form-inputs">
    <%= f.input :user_id %>
    <%= f.input :entity_type %>
    <%= f.input :entity_id %>
    <%= f.input :up %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
