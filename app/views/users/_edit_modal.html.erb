<%# Fields for an edit-user dialog %>
<%= modal_dialog :edit_user, (@user.channel? ? "Edit Channel" : "Edit User" ), response_service.modal_options do %>
    <%= render @user.channel? ? "form_channel" : "form" %>
<% end %>
