<nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse"
            data-target="#RP-navbar-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="/">RecipePower</a>
    <% if user_signed_in? %>
        <div class="collapse navbar-collapse navbar-left" id="RP-navbar-collapse">
          <ul class="nav navbar-nav">
            <%= navtab :collection, "Goodies", "/users/#{current_user_or_guest_id}/collection" do collection_menu_items end %>
            <%= navtab :friends, "Friends", users_path do friends_menu_items end %>
            <%= navtab :goodies, "Goody Bags", lists_path do goody_bags_menu_items end %>
            <!-- current_user.subscriptions(:public).collect { |l| stream_menu_item(l.name, list_path(l, format: :json), dom_id(l)) }-->
            <%= navtab :feeds, "Feeds", feeds_path do feeds_menu_items end %>
            <%= navtab :news, "News", "/users/#{current_user_or_guest_id}/news" %>
            <%= navtab :more, "More", "/users/#{current_user_or_guest_id}/biglist" %>
            <!-- menu_items << link_to("New Public List...", new_list_path( modal: true ))-->
            <%= navtab :home,
                       content_tag(:b, "&nbsp;#{current_user.handle}&nbsp;".html_safe, class: "glyphicon glyphicon-home")+
                       content_tag(:b, "", class: "glyphicon glyphicon-cog"),
                       user_path(current_user) do header_menu_items end %>
          </ul>
        </div>
    <% end %>
  </div>
  <% unless user_signed_in? %>
      <p class="navbar-text navbar-right active">
        <%= link_to_modal "Sign In", new_user_session_path,
                          class: "preload navbar-link",
                          style: "color: white; margin-right:20px" %>
      </p>
  <% end %>
</nav>
