<!--<label class="string optional">Child of</label>-->
<%= form_tag(associate_tag_path(presenter.tag, as: 'child'), class: 'submit', method: 'post') do %>
    Make child of:<br>
    <%= token_input_tag :other,
                        :tokenLimit => 1,
                        :noResultsText => 'No such tag found',
                        :tagtype => (presenter.tag.tagtype if presenter.tag.tagtype > 0),
                        :tagtype_x => [0, :List],
                        :query => { all: true, except: presenter.tag.id, verbose: true, disambiguate: true }, # See TagsController#match
                        :allowFreeTagging => false,
                        :allowCustomEntry => false
    %>
<% end %>
