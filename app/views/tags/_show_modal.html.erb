<% viewparams = @viewparams || (@filtered_presenter && @filtered_presenter.viewparams) unless defined?(viewparams) && viewparams %>
<% @tag = decorator.object %>
<%= modal_dialog :show_tag, "What We Know About #{summarize_tag true, false}" do %>
  <%= render 'show', decorator: decorator, viewparams: viewparams %>
<% end %>
