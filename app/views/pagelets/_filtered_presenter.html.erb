<% if @filtered_presenter.respond_to?(:entity_type) && @filtered_presenter.entity_type %>
    <%= render_item :collection %>
<% else %>
    <%= render_item :card %>
    <%= render_item :comments %>
    <%= render_item :owned %>
<% end %>
