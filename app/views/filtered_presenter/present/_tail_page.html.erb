<% viewparams = @viewparams || (@filtered_presenter && @filtered_presenter.viewparams) unless defined?(viewparams) && viewparams %>
<div class="page-item stream-tail <%= viewparams.stream_id %>" >
  <%= render 'filtered_presenter/present/tail', viewparams: viewparams %>
</div>
