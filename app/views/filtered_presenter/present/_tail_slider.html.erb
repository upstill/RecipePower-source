<% viewparams = @viewparams || (@filtered_presenter && @filtered_presenter.viewparams) unless defined?(viewparams) && viewparams %>
<div class="slider-item stream-tail <%= viewparams.stream_id %>">
  <%= render 'filtered_presenter/present/tail', viewparams: viewparams, trigger_check: "RP.slider.trigger_check" %>
</div>
