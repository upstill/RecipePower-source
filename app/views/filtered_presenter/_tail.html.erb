<% if next_path = @filtered_presenter.next_path %>
  <%= image_tag "ajax-loader.gif", class: "beachball hide" %>
  <%= stream_loadlink next_path, @filtered_presenter.stream_id, (trigger_check if defined?(trigger_check))  %>
<% else %>
  <%= empty_msg  %>
<% end %>
